---
ansible:
  playbook: tests/test.yml

verifier:
  name: serverspec

molecule:
  ignore_paths:
    - .git
    - .vagrant
    - .bundle
    - .vendor
    - .venv
    - .molecule
    - vendor

docker:
  containers:
  - name: ansible_role_plex_media_server_centos6
    image: centos
    image_version: '6'
